import { Component } from '@angular/core';

@Component({
  selector: 'app-restaurant-list',
  standalone: true,
  template: `
    <h1 class="text-2xl font-semibold text-slate-900 mb-2">Restaurants</h1>
    <p class="text-sm text-slate-500">
      Liste des restaurants — à implémenter à l'étape 4 (création, recherche, détail).
    </p>
  `,
})
export class RestaurantListComponent {}
