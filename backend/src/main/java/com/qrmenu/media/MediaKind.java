package com.qrmenu.media;

/**
 * Nature d'un média stocké. Phase 1 : seul {@code PDF} est produit/consommé
 * (menu BASIC). {@code IMAGE} existe dans le modèle pour la suite (photos produits).
 */
public enum MediaKind {
    PDF,
    IMAGE
}
